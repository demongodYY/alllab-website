import { History } from 'cell-router/source';
import { _ScenicSpotModel } from './ScenicSpot';
export const history = new History();

export const ScenicSpotModel = new _ScenicSpotModel({
    owner: 'EasyWebApp',
    repo: 'DashBoard'
});
